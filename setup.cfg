[bdist_wheel]
universal = 1

[coverage:run]
parallel=True
source=mycli

[tool:pytest]
addopts = --capture=sys
          --showlocals
          --doctest-modules
          --ignore=setup.py
          --ignore=mycli/magic.py
          --ignore=mycli/packages/parseutils.py
          --ignore=test/features
